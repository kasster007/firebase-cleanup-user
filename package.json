{
  "name": "firebase-cleanup-user",
  "version": "1.0.0",
  "description": "This repo contains the nodejs program to cleanup the exsiting user in firebase.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kasster",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^9.8.0"
  }
}
